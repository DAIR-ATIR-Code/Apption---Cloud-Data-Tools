@if(StatsData != null && StatsData.Count > 0)
{
    <div class="sample-container">
        @foreach(var stat in StatsData)
        {
            <div class="sample">@stat</div>
        }
    </div>
}

@functions
{
    [Parameter] public List<string> StatsData { get; set; }
}